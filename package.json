{
  "name": "funda",
  "version": "1.0.0",
  "description": "Fundamental Analysis",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "node-sass src/client/scss -o dist/client/css",
    "build-js": "babel src/client/js -d dist/client/js",
    "build-client": "npm run build-css && npm run build-js",
    "watch-css": "node-sass -w src/client/scss -o dist/client/css",
    "server": "babel-node src/server",
    "start": "[[ -d dist ]] && rm -r dist; npm run build-client && npm run server",
    "watch": "nodemon --watch src --exec npm run start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.16.2",
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.6.0",
    "express": "^4.15.3",
    "node-sass": "^4.5.3",
    "nodemon": "^1.11.0"
  }
}
